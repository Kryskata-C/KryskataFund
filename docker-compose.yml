version: '3.8'
services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "KryskataFund123!"
    ports:
      - "1433:1433"
    volumes:
      - sqlserver_data:/var/opt/lib/mssql

volumes:
  sqlserver_data:
